# 交接单（路线与效果决策）—— 给下一个会话/下一个人

> 写在 2026-09-15（用户重启电脑前）。**唯一目的是让新会话不必重读历史**：
> 读完这份 + `AGENTS.md` + `docs/plugins-compare.md` 第六节，就能接着往下走。
> 规矩、不变量、验证纪律**不在这里重复**，一律以 `AGENTS.md` 为准。
>
> ⚠️ 这份**不是**待办列表式的「计划」，而是一份**决策现场记录**：当前状态、已查证的事实、
> 还没拍板的问题。R11 的交接单是 `docs/handoff-r11.md`（那一轮已收口，v0.4.0 已发布）。

## 1. 现在的状态（已核对，不是叙述）

- 本地 == 远端 == `6bd98dc`，工作树干净。
- R11 已全部收口，`v0.4.0` 已发布；Mac 上的插件是软链，改代码即生效。
- 本轮**只改了文档**，**没有动任何代码**（用户明确要求「先不着急修，先把问题捋顺」）。

| 提交 | 内容 |
|---|---|
| `dea6147` | 修正对比表 5 行过期项（把已实现的能力标成 ❌）+ README 方言默认值漂移 + 新增 `plugins-compare.md` 第六节 |
| `6bd98dc` | **更正**第六节 6.1：「六家都要重装」是错的，必须分 A/B/C 三类（附源码与 INSTALL 原文凭据） |

## 2. 这一轮在决定什么

用户看了 aiduPOP（AP）的截图后提出两个问题，**都还没最终拍板**：

1. **「是不是我们的路线选错了？我们这条路是不是真的做不到那些效果？」**
   —— 已给出结论：**路线没问题**，截图里那些效果在我们这条路上**都能做**（逐条证据见
   `plugins-compare.md` §6.3）。用户对这个结论尚未明确表态。
2. **「如果他们能实现我们实现不了的效果，那他们那种方式我也能接受」**
   —— 用户**愿意接受 monkeypatch 路线**。这一条在 6.1 更正后**重新打开了**，见下。

## 3. 已查证的事实（这一轮的产出，别再重查）

### 3.1 六家的升级代价必须分三类（**我第一版写错过一次，见 `6bd98dc`**）

| 类型 | 谁 | 升级后要做什么 | 坏的时候什么样 |
|---|---|---|---|
| **A. 安装期改磁盘源码** | HFC / CLS / FC | **每次升级都要重跑 install**（`hermes update` 覆盖被改过的文件） | 明确：`verify` 报 `Incompatible` 或钩子消失 |
| **B. 运行时 monkeypatch** | AP / ALS / HLS | **什么都不用做** | **静默失灵**：私有名改名 ⇒ 只打 WARNING ⇒ 卡片链路整条消失，等作者发新版 |
| **C. 官方插件契约** | **我们** | **什么都不用做** | **fail-open**：退回纯文本 + 留日志，消息永不丢 |

**凭据**：CLS/FC 的 `patcher.py` 把 `<HERMES_HOME>/hermes-agent/` 下的 `run.py` 备份成
`run.py.hermes_lark.bak` 后改写磁盘；HFC 用 `install/patcher.py` + manifest + recovery。
FC 的 INSTALL 原文：「`Hooks gone after Hermes update`｜Hermes update **overwrote patched files**｜
`Re-run verify + install`」；CLS 的 Update 段是 `uninstall`（remove old injection）→ `verify` → `install`。

> **结论口径（重要）**：**「不用重装」不是我们独有的优势** —— B 类同样满足用户的原始要求。
> **我们相对 B 类的优势只剩两条**：① 不依赖任何私有名 ⇒ 不会随上游内部重构**静默**失灵；
> ② 一旦不适配**必定退化成纯文本并留痕**，而不是无声消失。**别再把「不用重装」当主要卖点。**

### 3.2 两条被截图误导的前提（都已复核）

1. **`(Recommended)` 不是 AP 的功能** —— 是 **Hermes 核心**加的（`tools/clarify_tool.py:16`
   `RECOMMENDED_LABEL`），AP 全仓零命中，我们免费得到。**此项无差距。**
2. **AP 的澄清「三态卡 + 重试提交」是三重死的代码**（子代理用它的真模块实测）：
   ① 点击入口的返回值**被核心丢弃**（核心把 `_on_card_action_trigger` 当绑定方法注册，
   类属性 patch 打不进去）；② 换卡要用的 SDK 类 import 路径错 ⇒ 实测 `ImportError` ⇒ 响应恒为
   `None`（全仓无 toast，失败时屏幕上什么都没有）；③ 即使修好，`_submitted_card_response` 传
   `choices=` 而构造器不收 ⇒ `TypeError` 被 except 吞掉 ⇒ 回落原生 ⇒ 点击变成注入会话的
   `/card {...}` 命令。它还**忽略 `resolve_gateway_clarify` 的返回值** ⇒ 超时后补点会刷出
   **假确认**。截图里「点一下跳已确认」是这条死路的**症状**（1 秒后服务端换卡 + 硬编码
   `sleep(1.0)`），**不是它比我们能干**。

### 3.3 我们真正落后的一处（用户第一眼就能看出来的那一处）

**默认的 2.0 澄清卡把选项文本藏在下拉里，卡面上看不到有哪几个选项。**
- 我们：`core/cards.py:1445` = `md(❓问题)` + 下拉 + 输入框 + 脚注。
- AP：`cardkit/special.py:157-166` 在下拉之上额外放一份 **markdown 选项列表**（截图中那
  A/B/C/D 四行是**纯文本、不可点**）。
- `docs/plan-6-effects.md:148` 本来就写了要做，**实现时漏了，且无记录**（是偏差，不是权衡）。
- 附带：默认 2.0 卡的脚注写着「**点按钮**，或直接回复文字都行」（`core/i18n.py:42`），
  而这张卡上**没有按钮** ⇒ 文案与形态不符。

### 3.4 修掉的一处「我们自己低估自己」（**教训要记住**）

`plugins-compare.md` 第二节「我们」列初版有 **5 行与实际相反**：多卡拆分、点击即回卡、
长文封卡分片、撤回/删除守卫、流式期间改结构 —— **五项全都已实现**（R4 卡链、澄清回填内联换卡、
R4 封旧开新、`_WITHDRAWN_CODES` 标死车道、cardkit 元素级写）。README 第 63 行也还写着
「当前默认 1.0」，与代码 `clarify_dialect="2.0"` 及本文件第 201 行自相矛盾。**两者都已修。**

> **教训**：这张表是对外门面，**过期的 ❌ 比空白更危险** —— 用户「感觉差距很大」的一部分
> 就是这张表写出来的。**改完代码后要回头看这两处文档。**

## 4. 差距清单（9 条，带证据/成本/不变量冲突）

**不在这里重复**，见 `docs/plugins-compare.md` §6.3。摘要（按性价比）：
1. 2.0 澄清卡**选项文本外显**（S）· 2. 脚注按方言拆两条（XS）· 3. **工具参数脱敏**（M）
· 4. 卡片 trace id（S–M）· 5. `/larkdeck status` 补 uptime/错误码 top-N/**回落纯文本次数**（M）
· 6. 探针 ⑮：2.0 `button` + 组件级 `behaviors` 真机点一次（M，需用户点）· 7. 澄清卡 markdown
escape（S）· 8. 判据函数的双向负向对照（S）· 9. 陈旧澄清卡自愈（L，**建议先不做**）。

## 5. 未决项（等用户一条条捋，**不要自行开工**）

1. **路线**：三类（A 改源码 / B 运行时 monkeypatch / C 官方契约）**选哪一种** —— 6.1 更正后
   这一条重新打开；B 类也满足「不用重装」，差别只剩「失效方式」。
2. **9 条差距清单的取舍**：哪几条做、哪几条不在乎。
3. **丢内容的 bug**：`core/adapter.py` 的 `_strip_core_progress` 在 **finalize 帧上也可能生效**
   （4 个条件里**没有** finalize 护栏）⇒ 极端情形下**只渲染前半段**，而核心对 finalize 是
   **乐观记账、不会再补发** ⇒ **静默丢正文**。已由子代理复现（`'前半段\n\n---\n后半段'` → `'前半段'`）。
   修法：加一条 finalize 护栏条件 + 一条单测 + 一条变异。**用户尚未批准动手。**
4. **我们自己的文档**是否还有别处低估/高估我们（用户已知这一条，我提议做一次全面核对）。
5. 更早搁置的：**NAS 部署**（`/opt/data/plugins/larkdeck`，装前先读 `docs/switch-from-hfc.md`）、
   **Phase C**（每工具一个元素，前置缺口见 `docs/plan-r11.md`）、成本展示、字号主题。

## 6. 下一步（新会话从这里开始）

1. **先读**：这份 → `AGENTS.md` → `docs/plugins-compare.md` 第六节。
2. **等用户捋完问题**，再动代码。用户当前明确状态是「**先不着急修**」。
3. 若用户批准第 3 条（丢内容 bug）：按项目纪律走「修 → 四门禁 → 全量变异 → 单独提交 → push」，
   并**换一个不同的子代理**做对抗审计（R11 的规矩）。
4. 动手做差距清单第 4/5 条（trace id / status 计数）前，**先复核 AP 的目标文件**：
   那两条的细节来自子代理报告，**我本人未逐行复核**（见 `plugins-compare.md` §6.5）。

## 7. HLS 那一列：取证**中止**（不是跑完），已抢救部分内容

**结局**：2026-09-15 18:34 由我**主动打断**（父 `3a59e17b` + 两个子代理）。原因不是慢，是**卡死**：

| 角色 | 步数 | 工具调用 | 压缩尝试 | 结局 |
|---|---|---|---|---|
| 父 `3a59e17b`（HLS+AP） | **127** | 151 | 1 次失败 + 重试中 | 打断（`turn/end: aborted`） |
| └ 子 `4a9957e7`（HLS 清单） | **96** | 107 | **6 次**（最后一次也失败） | 打断 |
| └ 子 `68ba6ede`（AP 清单） | **97** | 118 | 2 次 | 打断 |

**失败凭据**：`compaction/end` 带 `error: "summarization truncated at the token cap
(incomplete checkpoint)"`（父 18:30:33、子 18:33:30 各一次）—— **上下文太长，摘要被截断**。

**根因（教训，下次派子代理必须照做）**：我把范围给大了（「**HLS + AP 两家**的完整功能矩阵」），
它自己又往下派了两层（父 → 2 个清单 → 2 个文档扫描），**三层 agent 各跑到 ~100 步、全部撞上
上下文天花板**。⇒ **重派时的硬约束模板**：① 只查**一家**；② 明确写「**不许再派下级子代理**」；
③ 给步数上限（如 25 步）；④ 到点必须交报告，宁缺勿滥。

**已抢救的内容**（两个已跑完的孙代理报告全文，从会话日志直接读出）：

- **HLS 文档里没有任何真机凭据**：`实测` / `真机` / `真机验证` / `empirical` / `verified` 在全仓
  `.md` 与 `.py` 里**零命中**；唯一近似的是 `ARCHITECTURE.md:594` 的「from today's debugging」
  （**无日期**）。⇒ 再一次印证本项目「**不采信第三方 README**」的纪律。
- **它自己两份文档互相矛盾**：flush 周期 `ARCHITECTURE.md:161` 写 180ms、
  `technical-summary.md:479` 写 80ms，代码是 `FLUSH_INTERVAL_MS = 180.0`。
- **`docs/adapter-patch.py` 根本不是补丁脚本**：它是 Hermes 官方飞书适配器的 **vendored 副本**
  （6096 行 vs 装机 4320 行，漂移 4574 行），自己 `register(ctx)` 注册 `feishu` 平台，
  且**没有任何模块 import 它**（带横杠的文件名 + 全仓 grep 无引用）。
- **错误码**：14 个已带 `file:line` 定位；其中 `200860`（`feishu/client.py:140`）与 `300312`
  （`card/builder.py:60`）**两份文档里都没有**。
- **它的限制常量**：元素 200/195 · 30KB/20000B · 2400 字分片 · summary 截 120 字 ·
  表格 20/5 · 打字 70ms。
- **成本**走私有 `session_*` 属性；脱敏在 `state/tooluse.py:82-133`；澄清 2.0 `behaviors` 回调在
  `card/special.py:371-407`；分片是 **seal-only**（`card_flow.py:920-953`）。
- ⚠️ **HLS 仓库里没有任何测试 / CI / 探针脚本**（子代理实测）。
- 同样证实了 6.1 的 B 类判定：HLS 是 monkeypatch 架构，**其文档没有任何「升级会坏」的说明**。

**取证配方（下次要再挖任何子代理的产出都用它）**：

```bash
H="$HOME/Library/Application Support/dsh-desktop/harness/sessions/--Users-Zayn-Code-larkdeck--"
zstd -dc "$H/<agent-id>/session.jsonl.zstd"     # 事件流；assistant 正文在
#   type=="assistant/message" → data.message.content[] → 取 type=="text" 的片段拼接
```

⚠️ **会话日志在 `~/Library/Application Support/...`，是持久目录，重启不删** ——
所以「重启会丢子代理产出」这个担心**不成立**：真正会丢的只是**正在跑的内存状态**。

**对本文件其余结论的影响：无。** HLS 那一列在 `plugins-compare.md` 里仍是空的，
可日后按上面的硬约束模板重派一个窄范围子代理补齐，或直接接受缺失。
