# LarkDeck v0.6.0

CLS 观感改造：面板摘要、工具行与页脚重做，模型名/回合耗时移入状态优先页脚，
markdown 的 i18n 边界写清，并为 `<font color>` 增加可回退开关。

## 概要

- 折叠面板摘要改为 `💭 思考 Xs · 🛠️ 工具执行 · N 步`；模型名与回合耗时不再出现在
  面板标题，改到页脚（状态 → 耗时 → 模型 → 上下文 → 卡短码）。
- 工具行重做：每个工具使用独立图标与加粗英文动作词，带耗时与状态词；命令或 skill
  名另起灰色小字。
- 状态词/灰色标题/灰色细节支持 `<font color>`，但 **v0.6.0 默认关闭**：真机三类消费者
  视觉尚未确认，默认走无色降级，避免客户端不认时把标签原样画给用户。
- plain_text/i18n 节点继续跟随客户端语言；markdown 工具行动作/状态词为明确的语言固定
  边界（飞书 markdown 不承载 `i18n_content`）。

## 用户可见变更

- 面板标题不再显示模型与回合耗时；页脚承担这些信息，且状态在最前。
- 工具行状态词固定英文（`Succeeded` / `Running` / `Failed` / `Blocked` / `Timed out` /
  `Cancelled` / `Skipped`），完整提示句仍走 i18n。
- 新增配置 `panel_color_tags`，默认 `false`；确认飞书客户端支持 `<font color>` 后可显式
  设为 `true` 恢复彩色状态词。
- D1 边界：默认 CardKit 流式折叠态只显示「执行详情」；生产不写 panel header 局部更新，
  收尾/降级/`/stop` 走整卡替换。

## 验证

- `tests/test_units.py`：225/225 passed（venv 3.11.15）。
- `tests/check_override.py` / `check_hooks.py` / `check_clarify_e2e.py`：全部 EXIT=0。
- `tests/mutate_check.py --preflight`：389/389（381 变异 + 8 对照）。
- 全量变异 run 3（tree `fd96f90f...` / commit `ecf11df`）：EXIT=0，381/381 断言红，
  `🟢0 / 💥0 / ❓0`、非对照 `⚪0`、对照 8/8。
- 颜色默认翻转为 false 后，在 release 候选树上重跑四门禁与定向 `-k CLS`：32/32 断言红。
- Phase 0/1/2/3 均经过 3 个独立子 Agent 对抗审计并收敛；证据留档于 `docs/audits/cls-ui/`，
  汇总见 `docs/verify-log.md`。

## 已知边界

- `<font color>` 真机三类消费者视觉仍为 `pending`（接口探针全 `code=0`，渲染需用户截图
  确认）；这是本版默认 `panel_color_tags: false` 的原因。
- panel header 局部更新探针的最终裁决为 c：生产不实现，实时摘要列未来 Phase C。
- `theme: ap_lite`、`text_profile` 与无网关 cron 投递的真机视觉/端到端验证仍为 `pending`，
  沿用 v0.5.0 的登记。
- 审计 refs（`refs/audit/cls-ui/*`）是本地证据，不随发行版推送。
