# LarkDeck v0.7.2

真机反馈收敛：页脚去短码、加载指示改成「会动、无文字」、工具图标逐条对齐 CLS、
澄清卡表单提交契约、以及 14:44 那条「卡片 + 纯文本」并存的真根因修复。

## 用户可见的变化

### 1. 页脚不再有 🔖 短码，且显示**模型名**

用户口径（2026-09-21）：**「我从来没有提过这个要求」** —— 它当初是内部审计为「截图 ↔ 日志对齐」
加进来的。现在页脚字段对齐同类插件（CLS / aiduPOP / FC）：

```
✅ 已完成 · ⏱ 10.4s · 🤖 DeepSeek Flash · ctx 20.5k/1m
```

2026-09-22 追加口径：**「显示现在的好像是模型 ID，我想要做成显示模型名」** —— `🤖` 后面
不再是原始 ID：`deepseek-v4-flash` ⇒ `DeepSeek V4 Flash`、`nvidia/moonshotai/kimi-k3` ⇒
`Kimi K3`、`claude-sonnet-4-5-20250929` ⇒ `Claude Sonnet 4.5`（确定性格式化，不改语义）。
想自定义名字就写别名：配置 `model_aliases`，或 `~/.hermes/model_aliases.json`
（`{"子串": "显示名"}`，与 `hermes-fry-cards` 同一份文件、改文件即生效）。

短码**只保留在日志自检行**（`卡片=<6 位>`），截图与日志仍然可以对上；卡片头 / 正文 / 面板 /
折叠提示 / 收尾 patch **一律不出现**（`test_v4_17b` 按状态 × 有无 message_id 全枚举扫整卡 JSON，
再扫建卡实体、每一次元素 batch、收尾整卡 patch）。

### 2. 加载指示：会动、无文字

用户口径：「『正在加载上下文…』**不对**，aiduPOP 那个是会动、无文字的状态指示」。
现在建卡插入的元素逐字段对齐 aiduPOP `_loading_element`：

```json
{"tag": "div", "element_id": "loading_hint",
 "icon": {"tag": "custom_icon", "img_key": "img_v3_02vb_…", "size": "16px 16px"},
 "text": {"tag": "plain_text", "content": " "}}
```

那个 `img_key` 是 aiduPOP / CLS / FC 三家**共用的同一张动图**（三家都没有自己上传）。
真机探针（`om_x100b643abd6394b0dfa26a200d65018`）用户目视结论：**① 会动** ✅。
首个正文 token 到达即删该元素；删失败会**换号重试**（同 uuid 重发会撞飞书 `200770`），
最多 3 次后放弃并留 warning（收尾整卡本来就不含它）。

### 3. 顶部状态条默认关闭

`card_status_header: false`（用户口径「顶栏默认不显示」）。想要旧观感把它设回 `true`。

### 4. 工具图标逐条对齐 CLS + Hermes 真实工具名全覆盖

用户真机选版（三臂探针 `om_x100b6424d23e24a8c3368dfbdaad661`）：「**乙**（emoji 内联进文本）
是图标与文字对得最齐的，丙那行**没有换行**」⇒ 工具行改成把 emoji **内联进文本**（不再用
`div.icon`：飞书把它顶部对齐渲染）。

主表 **28 条逐条等于** CLS `streaming/tooluse.py::_TOOL_DESCRIPTORS`（含**顺序**：
精确匹配 或 `alias_` 前缀匹配，未知工具回落 `setting-inter_outlined`）。
唯一有意偏差登记在案：`terminal`（CLS 表里没有它，而 Hermes 的 shell 工具**就叫** `terminal`）。
主表**逐条对齐 CLS**之外，另有一张**登记在案的本地扩展表**覆盖 Hermes 真实工具名
（`delegate_task` 🤖、`execute_code` 💻、`memory` 🧠、`session_search` 🕘、`todo_list` ✅ …）——
审计 B 实测：只照抄 CLS 表时 60 个真实工具名有 29 个落通用兜底图标（用户配置里已启用的
toolset 都在里面），现在是 **0 个**。
`tests/check_cls_alignment.py` 直接解析 CLS 源码比对（CLS 侧改了别名，我们这边会红），
且已**纳入正式门禁**（缺席即 FAIL，不再「缺席就跳过」）。

#### 4.1 工具行图标：官方**线性**图标做「文本前缀」（2026-09-22 真机定版）

用户两轮真机口径：①「emoji 好看但有点**花里胡哨、颜色不统一**；CLS 那种**统一颜色看起来更高级**」；
②「CLS 只在一部分场景用了这些图标，我们的插件应该**更全面**」。

**横评（三臂探针 `tests/probe_icons_layout.py`，卡 `om_x100b6414825f3ca8c339ed0a7cef3e9`；
我按用户截图逐像素量了图标与文字的垂直中心）：**

| 臂 | 放法 | 垂直偏差 | 结果 |
| --- | --- | --- | --- |
| 甲 | 元素级 `div.icon`（CLS 同款） | 图标比文字**高 3px** | 淘汰（2026-09-21 那次「偏上」就是它） |
| **乙** | **`markdown.icon`（官方文档称「前缀图标」）** | **0px** | **✅ 用户选版** |
| 丙 | `column_set` 图标独占一列 + 居中 | 1px，但横向空 **179px** | 淘汰 |

**定版**：工具行 = `markdown` + `icon`（前缀图标）+ **线性 `_outlined`** + 统一 `color:"grey"`。
token 全部**逐个对飞书官方图标枚举页 `enumerations-for-icons` 查证存在**（1154 个里挑的；
写错名字客户端不渲染且不报错）——白名单冻结在 `test_units.py::_VERIFIED_LINEAR_TOKENS`。
`ICON_ALIASES`（28 条逐条等于 CLS）**仍是对齐判据的唯一真相**（`check_cls_alignment` 只读它）；
`tool_icon_token(name, token)` 只做**渲染层精化**：60+ 真实工具名不再挤 14 个 token
（`terminal` ⇒ `command_outlined`、`memory` ⇒ `organization-book_outlined`、
`cronjob_manage` ⇒ `alarm-clock_outlined`、`send_message` ⇒ `send_outlined`…）。

**「更全面」**（用户第 ② 条）：同一批线性图标现在也用在
**工具详情行**（`tool-indent_outlined`，替掉文字箭头 `↳`）、**错误块标题**（`warning_outlined`）、
**长回合折叠提示**（`more_outlined`）。面板标题保持 `💭/🛠️`（CLS 同款）；页脚暂不动
（一个 markdown 只能带一个前缀图标，四段都上图标要拆分栏，见 v0.7.3 登记）。

**可切回**：`tool_row_icon: "line"`（默认）/ `"emoji"`（2026-09-21 选过的 emoji 内联形态）。
两条路都有门禁（`test_v4_46`/`V4-56`/`V4-57`）与变异（`V4-46`/`V4-55`/`V4-56`/`V4-57`/`V4-58`/`V4-59`）钉住；
golden 夹具同步重生成，diff = 行为声明（工具行 `div`+emoji ⇒ `markdown`+`standard_icon`；
详情行 `div`+`↳` ⇒ `markdown`+`standard_icon`）。

### 5. 澄清卡：表单提交不再「空提交」

官方契约：`option` / `options` / `input_value` **只在组件未嵌入 form 容器时**返回；
一旦放进 form，答案只在 `action.form_value[组件 name]`（submit 按钮的 `value` 是空的）。
现在 `value` 为空、答案只在 `form_value` 的提交形态已经走 e2e（用**真 SDK payload 类**构造，
与真机 WS 反序列化同源）：能解除网关阻塞、原地回执换卡、重复提交只给 toast。
组件级 `behaviors`（当前生产卡形态）路径不变。

### 6. 修复：`panel_expanded: true` 在结构化卡片上被静默忽略

打开"面板默认展开"后，结构化卡片**仍然是收起的**（只有旧渲染器正常）—— 96 个配置组合里唯一的
不一致项。根因是 `panel_shell()` 的 `expanded` 形参默认写死 `False`、两个调用方都不传它。

### 7. 修复：`unified_panel=false` / `footer=false` / 「无过程数据不出空面板」曾被静默违反

降载阶梯（`_ld_fit_structured_card`）过去无条件把面板/页脚开关写回 `True` —— 于是
`send()` / `edit_message()` 这条**静态回退车道**上：系统提示类消息仍会带一个**点开什么都没有**
的「执行详情」空面板，`unified_panel=false`、`footer=false` 也不生效。现在降载阶梯只是
「**上限**」（只能关、不能开），静态车道的开关恢复生效。
（回合卡——native 帧 / 收尾 patch / `/stop` 重绘——**仍然保留面板**：顶栏默认关时，面板边框是
完成绿 / 出错红 / 中止黄的**唯一**载体，摘掉它用户就看不到状态色。）
另外 `/stop` 重绘的结构化终态卡现在会正确关掉流式态。

### 8. 修复：卡片与「⏳ Working — …」纯文本并存（灰气泡）

14:44 那条纯文本的**真根因**不是内核心跳，而是我们自己的卡片被飞书拒收后 fail-open：
`collapsible_panel` 的直接子元素曾经是 `plain_text`（非法）—— 工具步数 > 20 触发折叠提示时
`300313` ⇒ 收尾整卡 `200621` ⇒ 核心回落 `send()`。已改成 `markdown`（变异 `V4-33` 实红）。

**同日真机探针复验又揪出这条修复只做了一半**：换元素类型时，新的 `markdown` 节点带着
`text_color: "grey"` —— 官方 2.0 富文本字段表里**没有** `text_color`，服务端回
`200621 unknown property`，而且是**整卡被拒**（不是忽略该字段）⇒ 长回合依旧会掉纯文本回落。
（单测当时全绿：它只钉我们想要的形状，不钉服务端收不收这个字段。）P3.1 新增的工具详情行
用了同一写法；错误块前缀图标也挂错了位置（挂进 `div.text`，那里没有 `icon` 字段）。

已修：灰色正文统一走 `<font color='grey'>…</font>`（官方「彩色文本样式」，也是工具行状态色
一直在用的写法），错误块图标挪到**组件级** `icon`；新增**面板元素字段白名单门禁**
（`check_cardview._assert_panel_element_fields`：未登记 tag 直接红、`text` 里不许有 `icon`、
`markdown`/`lark_md` 不许有 `text_color`，一次报出**所有**越界字段），变异 `V4-60`/`V4-61`
实红。真机探针修复后发送成功（`om_x100b64159f75b0a0c2f35ecdf3f0d36`）；完整经过见
`docs/audits/v0.7.2/audit-round1.md` §8.7。

## 排障增强（非用户可见）

* **出站留痕**：`send()` 卡片成功 / 卡片失败回落 / `edit_message` 成功 / 回落 各留一行
  `出站=<card|edit|text> chat=… mid=… 前置='…'`（限流按 (kind, chat) 独立）——
  「这条消息到底以什么形态发出去的」现在可以直接读日志回答。
* 预加载提示删除失败有 warning 留痕（不再静默以为删掉了）。

## 门禁（本次发布的证据）

| 门禁 | 结果 |
| --- | --- |
| `tests/test_units.py` | **282/282**（约 4s） |
| `tests/check_override.py` | OVERRIDE OK（报 `larkdeck v0.7.2`） |
| `tests/check_hooks.py` | HOOKS OK |
| `tests/check_clarify_e2e.py` | CLARIFY E2E OK（含**真 SDK payload 类**的表单提交场景；去掉 6 处固定睡眠后 4.2s→2.0s） |
| `tests/check_cardview.py` | CARDVIEW OK（含独立字面量：图标表 / 真实工具名 / spinner 三字段） |
| `tests/check_cls_alignment.py --require` | CLS ALIGN OK（28 条逐条 + 顺序；缺席即 FAIL） |
| `tests/run_fast.py --full` | 六支 + own_body + 锚点预检全 OK，合计约 12s |
| 变异账本 | `tests/mutation-verdicts.json`：**482/482 可跳过（真跑 482 + 继承 0）、待跑 0**；`full_audit_at=37d2ab1` 是账本 `_meta` 字段（`--ledger-status` 本身只报覆盖率）；每条同时带三重指纹（代码区域 / 当年抓它的用例名集合 / helper 与夹具）⇒ 协议见 `docs/verify-log.md`「09-21 协议变更」 |
| `install.sh --copy` | 真跑过（`HERMES_HOME=/tmp/ldinstall`）：**11 个运行文件**全到位；本轮修掉了 FILES 漏 `core/cardview.py` 的阻断项 |

## 已知残余（登记在案，不在本版）

* 表单**容器**形态（`form` + `form_action_type:"submit"` + 组件 `name` + `form_value[name]` 解析）
  未实现 —— 当前卡用组件级 `behaviors`，真机已通。
* 澄清卡三态里的 `submitted`（含 retry 按钮）与 `confirmed` 服务端 patch、30min TTL 未做
  （现在提交后原地换成「已答复」卡）。
* `show_reasoning=true` 时面板会嵌套 `collapsible_panel`（默认 false 规避）。**已发真机探针**
  `om_x100b6427ec67d4a4de74424945f4ca0` 请用户确认内层能否展开 —— 回话前不要把这条写成「已验证」。
* 低层出站原语（native 建卡 / 澄清卡 / `/stop` 与 DEGRADE 的整卡 patch）还没有统一的
  「出站=X」留痕，目前只有 `send()`/`edit_message()` 两条上层路径有。
