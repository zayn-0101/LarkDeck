# 交接单（R11 进行中）—— 给下一个会话/下一个人

> 写在 2026-09-15 07:5x。**唯一目的是让新会话不必重读历史**：读完这份 + `AGENTS.md` +
> `docs/plan-r11.md`，就能从「下一步」那一节直接开工。
> 规矩、不变量、验证纪律**不在这里重复**，一律以 `AGENTS.md` 为准。

## 1. 现在的状态（已核对，不是叙述）

- 本地 == 远端 == `c281e30`，工作树干净。
- 今天（2026-09-14 晚 → 09-15 早）六笔，**每笔都过了四门禁 + 全量变异才推**：

| 提交 | 内容 |
|---|---|
| `87a9ece` | A3 每回合自检（收尾/失败各一行，带 `本回合帧` 与 `正文剥进度`） |
| `ce7745a` | A7 正文净化（**有证明**才剥核心叠加的工具进度块）+ 修好一直空转的 `progress_lines_in_body` |
| `aa11568` | A0+A6：锁 / `_INFLIGHT` / `HOOKS` / `COMMAND` 一并进程级共享；盒子键清单唯一真相；适配器合并类**不自套娃** |
| `840ba31` | A1 世代快照（`模块=…#id · 加载序号 · manager · home · 钩子回调数`） |
| `6eaed6e` | A1 实测入档：C 假设被排除；**双跑是偶发的** |
| `c281e30` | B3：正文失败那一支改从 `live_state` 出发（不再手抄字段） |

- 最近一次全量变异：**250/250 全红 + 8 对照全绿**；单测 180/180。
- Mac 网关（launchd `ai.hermes.gateway`）跑的就是这批代码（2026-09-14 22:49 重启加载）。

## 2. 真机闭环 —— ✅ 已结束（不要再让用户复现）

用户自己发了一条真回合（「最近 AI 行业有什么大事」，6 推理轮 + 6 工具调用），日志里那行自检：

```
回合自检：面板=有（rounds=6 tools=6）· 页脚=有（ctx 29.3k/1m · 3%）· 写卡帧数=23
         · 传输=cardkit · 本回合帧=21 · 正文剥进度=0 · 会话桶=1
```

⇒ 原三症状（**面板恒空 / 页脚不显示 / 累计 0 帧**）真机全部闭环。
⇒ `正文剥进度=0` 是**设计内的 fail-open**：那个回合模型**先调工具、后写回答**，工具阶段我们
手里还没有正文可当锚，判据整体退回「原样渲染」；核心在下一个正文增量到达时自己会清掉那些行
（用户回看终态「没有大问题」与之一致）。**别再为这件事让用户改核心配置**（见第 4 节）。

## 3. 下一步（按顺序，一小步一验一推）

1. **B2**（`docs/plan-r11.md` §3 的 B2 行是任务书）：
   `inner_code()`（从 `msg` 解析内层码：真机两种形状，解析不出**返回 `None`、绝不返回 0**）
   → `capacity_exceeded()`（`300305`/`300315`）→ `300315` 按「容量满 ⇒ 确定性失败、不重试、留痕」
   处置 → 码表补**实测出处** + `plan-v1` 附录 A 加行 → 明写「正文列仍是确定性失败 = FATAL」。
   ⚠️ append 形状的 `300315` 真机样本**还没取**（附录 F 里登记为未测），要取就得跑探针。
2. **B1**：滑窗写入守卫（共享盒子里的 `deque`），动机**如实**写成「为 Phase C 留余量」；
   两条边界各一条变异：跳过的写**不置 `ck_decor`**、**跳过 ≠ 失败**（不改本帧返回值）。
3. **B 收口**：重推附录 B 的四个数字（≈8.2 逻辑写/秒、单帧最坏 9 次调用…）并同步 `AGENTS.md` / `README` / `CHANGELOG`。
4. **小尾巴两条**（都记在这里，别忘）：
   * `README.md` 里「正文干净」那句要写实：**工具执行期间可能短暂出现核心的进度行，回答一开始就消失**；
   * `panel` 的空面板诊断：建卡 seed 帧**必然**为空，却会打一条「面板为空 · 诊断」（真机实测噪声），
     应改成**只在收尾帧仍为空时**才报。
5. **A2'**：等下次双跑发生时读世代快照的两条 `manager=` 是否相同（相同 ⇒ (A) force；不同 ⇒ (B) 两个 manager）。
   判据已在线上，**不要**去发 SIGUSR2（`chain=True` 且无前置处理器时可能落到默认动作 = 终止进程）。
6. **Phase C**：按成本复核决定做不做；做的话观感确认需要用户**在场**看一眼（推论 16：回看等于没问）。
7. **Phase D**：`/neat-freak` 收尾 → 发布（版本号按落点定，发布前三件套：全量门禁 + 全量变异 + 真机探针）
   → GitHub Release → **Mac 同步**（重启网关 + 自检行 + `/larkdeck` 版本自报）。

## 4. 仍然生效的用户决定（别推翻）

- **不改 Hermes 核心的任何配置**（`display.platforms.feishu.tool_progress` 那类一律不动；
  用户 2026-09-14 明确拍板，此后 `~/.hermes/config.yaml` 与改动前逐字节一致）。
- `tool_progress` 那件事**不要再提**：插件侧已按「有证据才剥」解决，剩下的只是文档措辞。
- **不要攒很多再 push**；每个阶段结束后换子代理做对抗性审计；`/neat-freak` 收尾照旧。
- **不要反复让用户复现/验证**（用户明说过这是浪费他时间）；取样一律先想能不能自己拿。

## 5. 环境上的坑（今天实测）

- **push 会失败**：`~/.gitconfig` 里有 `http.postbuffer = 524288000`，经本机代理 push 时报
  `LibreSSL SSL_connect: SSL_ERROR_SYSCALL`（而 `git ls-remote` 正常）。绕过：
  `git -c http.postBuffer=1048576 -c http.version=HTTP/1.1 push origin main`。
  用户尚未同意改那行全局配置。
- **一律用** `/Users/Zayn/.hermes/hermes-agent/venv/bin/python3` 跑测试（系统 python3 少了
  `lark_oapi` ⇒ CardKit 用例假红）。
- 全量变异约 25–30 分钟，输出很大：**重定向到文件再读尾巴**，别把整份输出拉进上下文。
