# v0.7.3 P4 已知问题登记（不阻塞本批发布）

## 长任务/多卡时：原生流回退留下中间卡 + 第一张卡执行详情面板空白

**用户报告（2026-09-23）**：发「v0.7.3 验证」后最终收到两张答案卡，中间夹一条
`⏳ Working — 3 min — iteration 14, terminal`；第一张卡片的「执行详情」面板展开后空白
（生成过程中曾有内容）。

**现场数据（Feishu message list + gateway log）**：

* 用户消息：`om_x100b6407c4b7b4a0b18b539eee9f04a`（10:25:43）；
* 卡片 1：`om_x100b6407c47450a4c2486c55ca4e9ff`（10:25:48）；
* Working 卡：`om_x100b6407db3f98b4c1198e72899932d`（10:28:47，`send 判定 turn=False keys=['_interim_send']`）；
* 卡片 3（最终答案）：`om_x100b6407d5fc2ca8c125c5be7c07dd3`（10:30:11）；
* gateway log 关键行：
  - `10:30:03,419 [larkdeck] 卡片正文世代漂移：stream_gen=1 bucket_gen=0（按空正文 fail-open，不串会话/回合）`
  - `10:30:27,370 [larkdeck] finalize 分叉：own_len=659 core_len=657 first=0 ...`

**初步判定**：长任务原生流回退/multi-turn 交错场景；`_ld_panel.snapshot()` 在最终整卡渲染时
可能已被后续回合数据顶掉，`_ld_cardview` 仍按 `unified_panel` 保留空面板（状态色的载体），
于是展开为空。与本批 x-small / 系统提示静默改动无直接因果（该路径未被本批修改），
属于既有面板生命周期/回退问题。

**处置**：登记 **v0.7.4** 专项（复现脚本 + 「最终整卡若快照无过程数据，则沿用本回合最后一次
非空面板内容或直接不出面板」二选一，需对抗审计）；本批不修。用户 2026-09-23 确认该现象
「只在长任务/多卡时出现」。
