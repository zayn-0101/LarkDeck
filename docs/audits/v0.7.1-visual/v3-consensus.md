# LarkDeck v0.7.1 V2.1 + V3 统一结论（心跳修复 + Result/Error + 推理 A 形态）

- V2 审计 A4/B4/C4 均 GO-WITH-CONDITIONS；本提交修其高优先级条件：
  - `_ld_stream_fail` 统一 cancel 全部心跳（失败退出不再终态写卡）；
  - finalize 先 cancel 心跳再发终态 patch（消除 tick 竞态）；
  - 心跳成功写补 `_ck_window_note`（计入滑窗配额）；
  - 心跳 finally 自清 `_LD_HEARTBEATS`；
  - `card_status_header=false` 告警文案更新（structured 下已生效）。
- V3 交付：
  - `post_tool_call` 采集 `result/error_type/error_message`（hooks.py）；
  - `panel.record_tool_finished` 入库前截断 600 字 + `redact_inline_secrets` 脱敏；
  - `_ld_cardview` 把 `result_block/error_block` 传入 `ToolStepView`；
  - `cardview.tool_step_elements` 输出 `**Error**/**Result**` + fenced 块（22px 缩进）；
  - 推理 A 形态（嵌套 collapsible_panel）与 `show_reasoning=false` 过滤已在 V1/V3 结构化路径生效。
- 门禁证据：
  - `run_fast --full` 全绿；test_units 246/246；preflight 432/432；
  - V2-1/V2-2、V3-1/V3-2/V3-3 全部实测红（V3 三条一次跑通）。
- 剩余（V4/外部条件）：
  - 默认切 structured、删 legacy、长回合/预算、P3 真机嵌套探针、窗口 1/2/3 真机截图；
  - 独立部署 worktree + 软链重指、窗口 0 `normal vs normal_v2` 探针；
  - 发布里程碑 push/tag/release 需用户确认。
