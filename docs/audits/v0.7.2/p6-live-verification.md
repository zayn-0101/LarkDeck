# P6 真机部署与探针（v0.7.2 面板 UX 批次）

**日期**：2026-09-22 · **提交**：`15d0e9f`（部署）→ 后续仅文档提交（见 `docs/verify-log.md`）

## 部署

* `.deploy` 已指向 **`15d0e9f`**（本批全部改动；部署树里确认存在标记常量 `SPINNER_TOOL_IMG_KEY`）。
* 网关重启，日志拿到 **`[larkdeck] 启动自检通过：Hermes 0.21.1 · feishu 平台已由 larkdeck 接管 ·
  native 传输 cardkit · 钩子 …`**（21:31:11），随后正常 `出站=card` ⇒ 真机跑的就是这一版。

## 用户真机目视结论（2026-09-22）

* **页脚三态逐字正确**（探针卡 `om_x100b641c07b6c4a0c3d46710a2971ef`，用户截图确认）：
  `✅ 已完成 · 12.3s · DeepSeek V4.1 Flash · ctx 55.6k/1m · 5%` /
  `… · cache 75% · api 7` / `… · ttfb 0.4s` —— **无 emoji 段前缀、无短码**，状态词 `✅` 保留。
* 工具行三态（运行中＝自制动图前缀 + 蓝 `Running`｜成功＝灰图标 + 绿 `✓`｜失败类＝保留词 + 红）、
  嵌套轮「已结束收起 / 当前展开」、收尾定稿（纯推理轮收起）均与冻结口径一致。
* **A1 关键协议前提已在真机验证**：把面板**手动收起**后，我们发一帧生产形态 partial
  （内容变化、**不带 `expanded`**）⇒ 面板**仍保持收起**（卡 `om_x100b6412c77ddca8c3b79b34234a6ec`，
  第 2 帧截图：箭头朝下、内容隐藏、标题已变「第 2 帧」）。
* **动图位置的最终拍板**：**运行中＝自制动图做前缀、完成后＝原来的线性图标**
  （用户原话：「算了，用原来的方式吧，动态图放最前面，进行中用这个动态效果，完成后变成之前的 icon」）
  ⇒ **不改代码**，`adea7cb` 那轮全量重验继续有效。

## 被否掉的替代方案（如实记录，不留半成品）

| 方案 | 结果 | 证据 |
| --- | --- | --- |
| markdown 内联图片 `![ ](img_key)`（把动图写在 `Running` 后面） | ❌ 真机渲染成**块级整行大图**，`Running` 被挤到下一行 | 卡 `om_x100b6413a6bc30a8c46ec4daacba229`，用户截图 |
| `column_set` 两列（左文字 / 右 16px 动图，A/B/C 三变体） | ❌ 用户选择保留原形态 | 卡 `om_x100b641c6711eca0df3bdfc176984c4` |

## 探针卡自身的修正

用户反馈「测试卡看不到页脚」——探针卡是**手拼对照卡**，原先 ④ 只写成文字说明、没放页脚元素。
已改成**三个真实渲染的页脚元素**（与真实回合同源：都走 `cards.footer_line`），用户截图确认可见 ✓。
另单独核实：**生产卡片一直有页脚**（构建器直验 `body.elements` = `[answer, panel, footer]`，
footer 内容 `✅ 已完成 · 12.3s · M · ctx 1k/2k · 5%`）。
