# 安全政策

## 上报漏洞

请不要开公开 Issue，也不要在讨论区粘贴利用细节。

优先使用 GitHub 的私下漏洞上报（仓库 **Security → Report a vulnerability**）。
如果该入口不可用，请发邮件到：

**`zayn-0101@users.noreply.github.com`**


上报时尽量附上：

- 受影响的 LarkDeck 版本与 Hermes 版本；
- 复现步骤或最小样例；
- 影响判断（例如是否泄露凭据、真实 chat_id，或影响消息投递）；
- 你希望的披露时间。

## 支持版本

只支持最新 release tag。旧版本不保证单独打补丁：修复会进入下一个发布版本，
用户升级到最新版即可。0.x 阶段可能有破坏性配置变更，升级前请先看对应 release note 的
兼容与回退章节。

## 响应预期

- **3 个工作日内**确认收到，并给出一个私下的跟踪渠道；
- **7 个工作日内**完成初步评估并同步修复计划；
- 修复或缓解方案可用后，与上报者协调公开披露时间；在修复发布前，请对细节保密。

以上是目标而非合同。若长时间没有回复，请再发一封提醒，不要直接转公开。


## 范围

LarkDeck 以 Hermes 插件形式运行在用户本机，处理飞书消息、卡片内容与本地配置。
凭据由 Hermes / 用户环境管理，本仓库不应存储任何真实凭据、`config.yaml`、`.env`
或含真实 ID 的日志。发现仓库中误提交了这类内容，也请按安全漏洞上报。
